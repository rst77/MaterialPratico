apiVersion: v1
kind: Pod
metadata:
  name: simple-node
spec:
  containers:
    - image: localhost/simple-node
      name: simple-node
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP
